{
  "name": "pendulum",
  "version": "1.2.0",
  "description": "",
  "license": "MIT",
  "author": "",
  "type": "module",
  "main": "dist/index.mjs",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/index.mjs",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "imports": {
	  "#types": "./src/Types.mts",
	  "#genericMachine": "./dist/Core/StateMachines/Machines/GenericMachine.mjs"
  },
  "dependencies": {
    "yaml": "^2.8.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^6.0.0"
  }
}
